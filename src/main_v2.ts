/**
 * src/main_v2.ts
 * Entry point for the modern UI (v2)
 */

import { initUI } from "./ui_v2";

// Initialize the UI when DOM is ready
document.addEventListener("DOMContentLoaded", () => {
  initUI();
});
